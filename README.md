# bbmonitor
Clean, production-ready Bug Bounty scope monitor written in Go. Concurrent fetch of public BB programs, SQLite + JSON export, diff on every change, Telegram/Webhook notifications, TUI + daemon.
